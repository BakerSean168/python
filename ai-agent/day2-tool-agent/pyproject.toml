[project]
name = "day2-tool-agent"
version = "0.1.0"
description = "Day 2: Tool calling agent — hand-written, no framework"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31.0",
    "python-dotenv>=1.2.2",
]
