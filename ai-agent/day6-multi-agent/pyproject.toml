[project]
name = "day6-multi-agent"
version = "0.1.0"
description = "Day 6: Multi-agent writing team — researcher, writer, reviewer"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31.0",
    "python-dotenv>=1.2.2",
]
